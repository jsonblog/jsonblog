<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{#if post}}{{post.title}} - {{/if}}{{#if page}}{{page.title}} - {{/if}}{{blog.site.title}}</title>
  <meta name="description" content="{{blog.site.description}}">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css">
  <link rel="stylesheet" href="/main.css">
  <link rel="alternate" type="application/rss+xml" title="{{blog.site.title}} RSS Feed" href="/rss.xml">
</head>
<body class="max-w-wide mx-auto my-8 px-8">
  <header class="editorial-header animate-fade-in">
    <div class="h-1 w-24 bg-gradient-to-r from-transparent via-burgundy to-transparent mx-auto mb-8"></div>

    <h1 class="site-title">
      {{blog.site.title}}
    </h1>

    <p class="site-description">
      {{blog.site.description}}
    </p>

    <nav class="site-navigation">
      <a href="/">Home</a>
      {{#each pages}}
        <a href="/{{slug}}">{{title}}</a>
      {{/each}}
    </nav>

    <div class="h-0.5 w-full bg-gradient-to-r from-transparent via-neutral-300 to-transparent mt-8"></div>
  </header>

  <main class="content animate-fade-in">
    {{> content}}
  </main>

  <footer class="mt-16 pt-6 border-t border-neutral-200 text-center">
    <div class="font-sans text-sm text-neutral-600">
      Built with <a href="https://jsonblog.dev" class="text-burgundy hover:text-burgundy-dark">JSON Blog</a>
    </div>
    <div class="mt-2 font-sans text-xs text-neutral-500">Generated by {{generatorName}} v{{generatorVersion}}</div>
  </footer>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
  <script>hljs.highlightAll();</script>
</body>
</html>
