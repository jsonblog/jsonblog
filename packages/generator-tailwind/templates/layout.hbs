<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{#if post}}{{post.title}} - {{/if}}{{#if page}}{{page.title}} - {{/if}}{{blog.site.title}}</title>
  <meta name="description" content="{{blog.site.description}}">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css">
  <link rel="stylesheet" href="main.css">
  <link rel="alternate" type="application/rss+xml" title="{{blog.site.title}} RSS Feed" href="/rss.xml">
</head>
<body class="max-w-[816px] mx-auto my-8 px-8 text-[19px] leading-relaxed">
  <header class="mb-12 pb-8 border-b border-gray-200 animate-fade-in">
    <div class="text-2xl font-semibold mb-2 tracking-tight text-gray-900">{{blog.site.title}}</div>
    <div class="mb-5 text-gray-600 text-[0.95rem] leading-normal">{{blog.site.description}}</div>
    <nav class="mt-5 text-[0.95rem] space-x-6">
      <a href="/" class="font-medium text-gray-900 no-underline transition-colors hover:text-[#0066cc]">Home</a>
      {{#each pages}}
        <a href="{{slug}}.html" class="font-medium text-gray-900 no-underline transition-colors hover:text-[#0066cc]">{{title}}</a>
      {{/each}}
    </nav>
  </header>

  <main class="content animate-fade-in">
    {{> content}}
  </main>

  <footer class="mt-12 pt-6 border-t-2 border-gray-300 text-sm text-gray-600 leading-normal">
    Built with <a href="https://jsonblog.dev" class="text-[#0066cc]">JSON Blog</a>
  </footer>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
  <script>hljs.highlightAll();</script>
</body>
</html>
